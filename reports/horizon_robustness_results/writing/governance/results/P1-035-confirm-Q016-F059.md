Both items reviewed under the read-only confirmer constraints (no writes, no git, no files opened under the Temp\2\claude path, and only MANIFEST.json opened inside P1-W2-agent-scratch/).

```
Q-016 ORDERING: CONFIRMED
Q-016 CHOICE: (a)
F-059: CONFIRMED
F-059 CROSS-READ SHOWN: unknown
```

## Reasons (with locators)

**Q-016** (`Q-016.md:20-32`): every sub-rule of (a) narrows an exemption that (b) leaves open, so (a) forces strictly more numbers to trace, matching the card's own stated metric (`Q-016.md:20`). Hyphen/slash: (a) exempts a following number only after a letter (`Q-016.md:24`), so "3.23-3.35" traces fully under (a) but not under (b) (finding 7, `P1-026-code-review-checks.md:33`). digits/digits: (a) counts both integers (`Q-016.md:25`), fixing the `k/3` contradiction (`P1-026:33`). 0–10 integers: (a) suspends the exemption when the claim cites a count/bool anchor (`Q-016.md:26`), closing the false "3/3" pass-count gap (`P1-026:34`; `ho.pass.<ARM>.count` is unit `count`, `ANCHOR_SPEC.md:37`). Prefixes: (a) needs a word boundary before the prefix (`Q-016.md:27`), closing F-053 (`P1-026:30`). Plan §4.2 (`WRITING_ORCHESTRATION_PLAN_20260914.md:158-160`) assigns exactly this ordering check to a non-ORCH agent.

**F-059** (`findings.jsonl:117`): both briefs restrict only named repository paths — A: `independent/`, `recompute_anchors.py`, `governance/results/` (`P1-anchors-A.md:67-69`); B: `anchors.json`, `recompute_anchors.py`, `independent/`, `governance/results/` (`P1-anchors-B.md:66-69`) — yet both invite running the script outside the repo without naming a private location: A "under your own session scratchpad" (`P1-anchors-A.md:82`), B "a temporary location that is not in the repository" (`P1-anchors-B.md:84`). Neither bars reading that shared location or another agent's files there. Plan §4.5's DBR rule (`WRITING_ORCHESTRATION_PLAN_20260914.md` §4.5) requires only reply-only, write-nothing parallel agents and does not constrain the run location, leaving this gap in the brief itself. `P1-028-anchors-B-failed.md:28-35` confirms both builders in fact used the same ORCH session scratchpad, with interleaved timestamps (A's `out_A_v1.json` 13:29, B's `out.json` 13:30, A's `anchors_A.py`/`out_A.json` 13:32), and states blindness "cannot be proven." Dispatch log seq 27/28 (`dispatch_log.jsonl`) record no isolation beyond the prompt file; the retry's private-directory/scratchpad-ban fix (`P1-028:39-41`) is added only afterward, confirming it was absent as dispatched.

Cross-read: `MANIFEST.json` and `P1-028` give only names, sizes, mtimes, sha256 and ORCH's own "(likely …)" attribution guesses — no logged access event or reply text shows either builder opening the other's files. Opportunity is shown; an actual read is not.

## Q-016 coverage examples

"HIST_ACT 0.069–0.179 pp" (claim-register row "Margin reading"): (b) hides "0.179" behind the hyphen; (a) traces both ends. "3/3 against 0/3" (same row): (b) exempts both unconditionally (≤10); (a) forces tracing because the row cites a count-unit anchor. Residual risk under (a): "(handoff §11 Q2)" (row "Target definition and incumbent headroom") — "Q2" has no hyphen, so it fits neither the hyphenated "Q-" card-id pattern (unlike "Q-005" in the same row) nor the bare-letter prefix list (F, H, D, U, P, S); it escapes tracing only via the fallback 0–10 rule, valid only while that row cites no count/bool anchor — a narrower protection than hyphenated ids get. Citation years (e.g. "2025") are unaffected by either option: they are exempted by a separate, unfixed mechanism (`P1-026` finding 9, `CC:148`) regardless of the Q-016 choice.
