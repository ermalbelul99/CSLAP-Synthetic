The map is written, but gate P1:G5 still fails on 4 rows marked UNMAPPED. `check_claims.py --coverage` flags only those 4: every other row resolves to a real claim or forbidden entry, and all three source documents are named. `--schema` returns CLAIMS SCHEMA PASSED. No text exceeds 40 words. I ran no git command.

**File:** `C:\ermal\CSLAP_Full_Project\CSLAP-Synthetic\reports\horizon_robustness_results\writing\evidence\requirements_map.json` has 82 rows: 65 map to a claim, 13 to a forbidden entry, 4 are UNMAPPED.

**Rows per source:**
- **Reviewed execution plan, 44:** §2 corrections 15, what carries over 7, data contract 15, §6 Step 4 7.
- **Campaign predeclaration, 9:** §5 5, §9.4 4.
- **Plan review ledger, 29:** 25 meet the brief's location rule. I added 4 more whose finding text is claim-bearing though their location column doesn't name one (lines 102, 149, 166, 174).

**UNMAPPED rows:**
- **RQ-027** (plan line 187): the large-order filter sets the decision pool only; small orders stay in evaluation. Fix: add to C-26 "The large-order filter selects only the movable decision pool; orders of every size stay in the evaluation stream, workloads and visits."
- **RQ-029** (plan line 191): "Preserve the documented duplicate/freeze-mask edge case." Fix: a C-26 disclosure qualifier stating the one-product edge case (DATA_PROVENANCE.md:27) and that the companion's frozen/movable split is kept.
- **RQ-063** (ledger line 66, RT1 "Likely-missed gaps 1-10"): the gap texts are in no file I may read, so no claim can be chosen. Q-card, Class E: ORCH reads RT1's record and splits the row. Default: stays UNMAPPED.
- **RQ-064** (ledger line 84, PR1 finding 12, F15 ck_ext checks): this is a P7 tooling requirement that no claim or forbidden wording should carry. Q-card, Class J. Options:
  - (a) keep it UNMAPPED and route it to P7 (conservative default);
  - (b) drop it from the map.

**Rows that depend on cards:**
- **RQ-048** maps to C-17 as written, because Q-020 is open.
- **RQ-046** (C-20) and **RQ-056, RQ-076, RQ-079** (C-10): these claims write δ values in words under Q-019's default until wave P1-W7.
- Q-018 affects no row.
