STATUS: REVISE_WRITING

Findings (BCL round 2, red team; register under review is `writing/evidence/claims.json`/`claims.md`, `requirements_map.json`, `document_values.json`, `interpretation_errata.md`)

**F1 — Major. Incumbent comparison in C-01 omits the headroom qualifier (item: "comparison with the incumbent without the headroom qualifier").**
Artifact: `writing/evidence/claims.json` / `claims.md`, claim C-01, `allowed_wording[3]` and `required_qualifiers`.
Claim: the approved wording "On this maximum-deviation statistic HIST+ACT-T, at most 1.160 percentage points over the seeds, is close to the incumbent's 1.157 on the same window..." compares an optimized arm directly against the incumbent's realized maximum station deviation.
Evidence: C-09 states the mechanism that makes this comparison misleading on its own — "targets b_s are the incumbent layout's own historical station shares... so the incumbent starts with the whole allowance as headroom while an optimised layout spends it," with its required qualifier "State the target definition and this asymmetry before any result that compares arms with the incumbent" (`claims.md` C-09). But that qualifier lives only in C-09's own `required_qualifiers` list; C-01's four `required_qualifiers` (visit-vs-optima, bundling, descriptive-not-endpoint, scope paragraph) do not include it. A writer using C-01's approved sentence in isolation, and a mechanical `check_claims.py` pass that checks qualifiers per-claim, will not be forced to disclose that the incumbent's low deviation is structurally guaranteed (near-zero required slack on its own target) rather than a demonstrated equivalence in robustness. This is the exact overclaim the brief flags: "close to the incumbent" reads as parity without the reader learning the incumbent cannot fail this particular statistic by construction.
Fix: add C-09's headroom qualifier to C-01's `required_qualifiers` (discharge: sentence) wherever the incumbent's 1.157 is cited, or forbid citing `ho.maxdev.incumbent` from C-01 without co-locating the C-09 statement in the same paragraph.

**F2 — Minor. Forbidden-wording gaps: synonym variants survive the block list.**
Artifact: C-02 forbidden list ("replicated", "confirms the exploratory finding") and C-13 forbidden list ("twice the companion's saving").
Evidence: near-synonyms are not listed and would pass a literal string check: "reproduces the exploratory finding," "corroborates the earlier result," "the held-out pass bears out the primary endpoint" (for C-02); "roughly double," "nearly double," "about double the companion's out-of-sample saving" (for C-13). None of these trip the current forbidden strings but carry the same overclaim.
Fix: extend both forbidden lists with the synonym families above before P1:G-checker sign-off.

**F3 — Minor. Q-008 conditional-forbidden wording depends on unstructured resolution prose.**
Artifact: C-06 `forbidden_wording` entries conditioned on `{"q_card":"Q-008","outcome":"w_greater_than_w_star"}`; `Q-008.md` resolution line.
Evidence: the resolution is confirmed ("w ≈ 0.0900 exceeds w* ≈ 0.0028... stays forbidden"), so "substantial," "despite," and "beyond the historical maximum" are live-forbidden now. But the only machine-readable signal is a sentence buried in Q-008's free-text `Resolution:` field, not a structured flag; nothing prevents a downstream automated pass from mis-parsing or skipping it. Given C-06 also lacks a finalized replacement wording (Class J pending SCI-5), this is the single highest-risk claim in the register for the drift narrative and should not rely on prose-parsing.
Fix: add a structured `resolved_outcome: w_greater_than_w_star` field to the card or to `claims.json` directly, and block writing of C-06's drift paragraph until SCI-5's Class J wording lands.

**F4 — Q-020, red-team position on C-17 negative-results classification.**
I argue for option (b): classify the held-out misses of NOM and HIST+ACT as a genuine failure of the no-margin policy, not solely a "demonstrated boundary condition."
Evidence: (i) C-03 — every no-margin arm missed the band on every one of three seeds (0/3), with NOM breaching both cap and floor on every seed; (ii) C-04 — both no-margin arms' returned layouts used essentially their entire training allowance (0.019997–0.019999 of 0.02), leaving no slack, so any deviation from the training scenario was mechanically certain to breach; (iii) `CAMPAIGN_PREDECLARATION.md` §5 (RQ-048) commits to reporting "a negative or mixed result... as such," favoring direct language over euphemism. A 0/3 pass rate from a policy predeclared as the "primary, data-supported" setting is the first thing a referee will call a failure; labelling it only a "boundary condition of the conditional guarantee" reads as softening a result the plan's own reporting commitment tells the authors to state plainly. Option (b) also better supports C-30's practitioner recommendation (margin policy is "tested... needing validation"), since the contrast is sharper if the no-margin baseline is named as failing rather than merely falling outside a technical uncertainty set.

**Hard stops.** No claim in the register depends on new experiments, solves, or data collection; every gap identified above (F1–F3) is resolvable by adding or restructuring qualifiers/forbidden-wording entries in the evidence files, and Q-020 (F4) is a wording judgment on already-anchored evidence. Nothing here should route to REVISE_METHOD, REVISE_CODE, or MORE_TESTING.

Relevant paths: `C:\ermal\CSLAP_Full_Project\CSLAP-Synthetic\reports\horizon_robustness_results\writing\evidence\claims.json`, `...\claims.md`, `...\interpretation_errata.md`, `...\writing\governance\questions\Q-008.md`, `...\Q-020.md`, `...\writing\governance\results\P1-040-register.md`.
