```
Q-020 ORDERING: CONFIRMED (b) > (a)
```

Reasoning (locators):

Per the brief's own definition, "more conservative" = claims less for the tested method, concedes more to a skeptical referee — not "epistemically most hedged."

- (b) names the held-out misses of NOM and HIST+ACT "a genuine failure of the no-margin policy" — a blunt admission of shortcoming for a tested arm.
- (a) keeps C-17 as written: the same misses stay classed only as "empirical limitation... mechanism is not isolated" (`W/evidence/claims.json`, C-17 `statement`) — softer, avoids the word "failure," and is the more self-protective reading even though it hedges more on causal mechanism.
- (b)'s admission is a bigger concession by the definition given, so it outranks (a) even though (a) is more cautious about attributing cause. This mirrors Q-016's precedent (`W/governance/questions/Q-016.md`, Resolution): "conservative" there meant the stricter standard on the paper's own claims, not the option that asserts least overall.
- (b) is scoped to "the no-margin policy," distinct from C-17's `forbidden_wording` "the method fails" / "robust optimisation fails" (`claims.json`), so it doesn't itself breach the register.
- `WRITING_ORCHESTRATION_PLAN_20260914.md` §4.3 line 230: on no majority, "Ordered cards adopt the most conservative option" — the ordering is the tie-break, so (b) as fallback is the cautious choice.

Ambiguity: (b)'s singular "the no-margin policy" (Q-020.md line 2) spans two distinct arms (NOM, HIST+ACT) that differ in scenario/activation logic and share only the absent margin; a reader could misread this as one unified policy. Clearer wording: "(b) classify the held-out misses of each arm without a reserved margin (NOM and HIST+ACT) as a genuine failure of that arm's no-margin design."
