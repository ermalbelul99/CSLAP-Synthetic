# Remote CPLEX machine -- daily workload-feasibility robustness study.
# CPLEX is the only backend; highspy is deliberately absent.
numpy>=1.21.0,<2.3.0
pandas>=1.3.0,<2.3.0
matplotlib>=3.4.0

# Exact MILP backend. `cplex` needs a licensed local install; the community
# edition caps model size and will not solve the industrial instance.
docplex>=2.25
cplex>=22.1
